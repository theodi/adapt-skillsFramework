define([
  'coreJS/adapt',
], function(Adapt) {

    var skillsFramework = _.extend({
  
    initialize: function() {
    	// Does nothing
    }

  }, Backbone.Events);
  
  skillsFramework.initialize();
  return (skillsFramework);
});
